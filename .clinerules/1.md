# Translate Russian text from chat

The user has typed Russian text after the `/1` command in the chat message.

1. Extract everything after `/1` in the user's message — that is the text to translate.

2. Translate it to professional technical English. Apply these rules strictly:
   - ПКМ → right-click (RMB)
   - ЛКМ → left-click (LMB)
   - СКМ / MMB → middle mouse button (MMB)
   - двойной клик → double-click
   - перетаскивание → drag
   - наведение → hover
   - контекстное меню → context menu
   - кнопка → button
   - узел → node
   - ветка → branch
   - рамка → border / frame (use context)
   - холст → canvas
   - Keep all code references, file names, variable names, and emoji exactly as-is.
   - Keep Markdown formatting if present.

3. Reply with ONLY the translated text — no explanations, no preamble, no "Here is the translation:". Just the result.
