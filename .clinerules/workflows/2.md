# Translate BACKLOG.md to English

1. Open the file `BACKLOG.md` in the project root and read its full contents.

2. Find all text written in Russian (Cyrillic characters). Leave already-English text untouched.

3. Translate all Russian text to professional technical English. Apply these rules strictly:
   - Preserve all Markdown formatting (bullet points, bold, headers, checkboxes).
   - Translate UI interaction terms correctly:
     - ПКМ → RMB (right mouse button) or "right-click"
     - ЛКМ → LMB (left mouse button) or "left-click"
     - СКМ / MMB → MMB (middle mouse button)
     - двойной клик → double-click
     - перетаскивание / drag → drag
     - наведение → hover
     - контекстное меню → context menu
   - Keep all code references, file names, and technical identifiers exactly as-is (e.g. `BACKLOG.md`, `nodes[]`, `src/`, `type='root'`).
   - Keep all emoji as-is.
   - Do not add, remove, or reorder any tasks — translate only.

4. Write the translated content back to `BACKLOG.md`, replacing the original.

5. Reply in chat with exactly: `/2 OK`
