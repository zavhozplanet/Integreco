# INTEGRECO — КОНТЕКСТНАЯ ПАМЯТЬ ПРОЕКТА

> **Версия контекста:** 2.2  
> **Дата обновления:** 2026-04-05  
> **Актуальный исходный файл:** `src/` (модульная архитектура)  
> **Пользователь:** Андрей (русскоязычный, нетехнический, Linux + Chrome/Wayland)

---

## 1. СУТЬ ПРОЕКТА

**Integreco** — интерактивная майнд-карта для визуального мышления, построения концептов и графов взаимосвязей.

**Ключевые принципы:**
- Минимум текста в интерфейсе — максимум инфографики (значки, иконки, символы) для универсальной доступности
- Работа на десктопах и тачскринах
- Офлайн-first с локальным хранением
- Вдохновление: Mindomo, Obsidian

**Планы масштабирования:**
- PWA для всех платформ
- Плагин для Obsidian
- **Multi-Nodes (Гибридные блоки):** Масштабируемые узлы с поддержкой произвольных точек подключения на границах и адаптивным поведением текста/скролла.
- **Интеграция с ИИ** (в планах)
- API для сторонних сервисов
- Синхронизация устройств через Syncthing (ближний план) и PouchDB/CouchDB (дальний план)

---

## 2. СТЕК И АРХИТЕКТУРА

### Текущее состояние
- **Модульная архитектура** — логическая часть разбита на `src/*.js`, стили в `styles/main.css`. `index.html` используется как минимальная оболочка для подключения скриптов в глобальный `window`-контекст.
- **Vanilla JS + SVG**, функциональные модули без бандлеров.
- Узлы — `<div class="node">` с абсолютным позиционированием на `#canvas` (8000×8000px)
- Линии/рёбра — SVG на `#svgl` (8000×8000, поверх canvas, `pointer-events:none`)
- Трансформация: `canvas.style.transform = translate(panX,panY) scale(zoom)`, `transform-origin:0 0`
- Шрифт: Inter (Google Fonts)
- Персистентность: `localStorage`
- PWA: Service Worker (`sw.js`) + `manifest.json`

### Файловая структура проекта
```
Integreco/
├── index.html          # Минимальная оболочка (подключает JS-модули и CSS)
├── styles/
│   └── main.css        # Основные стили проекта
├── src/                # Разделенные логические JS-модули (> 20 файлов)
│   ├── init.js         # Инициализация и автосохранение
│   ├── state.js        # Глобальное состояние (nodes, edges, настройки)
│   ├── geometry.js     # Геометрия Безье рычагов, snapping, render()
│   └── utils.js...     # Функции управления, UI, события мыши/тачскрина
├── sw.js               # Service Worker (v2) для офлайн-кэширования модулей
├── manifest.json       # PWA-манифест
├── package.json        # Конфигурация (ВНИМАНИЕ: содержит React/Tailwind — артефакт AI Studio)
├── vite.config.ts      # Конфигурация Vite (артефакт AI Studio)
├── .env.example        # Пример переменных окружения
├── README.md           # Инструкция запуска
└── PROJECT_CONTEXT.md  # ← ЭТОТ ФАЙЛ (контекстная память)
```

> ⚠️ **ВАЖНО:** `package.json` содержит зависимости React, Tailwind, motion и т.д., оставшиеся от среды Google AI Studio. Проект НЕ использует React — это чистый vanilla JS. При рефакторинге нужно либо очистить package.json, либо перевести проект на модульную архитектуру.

---

## 3. СТРУКТУРА ДАННЫХ

### Node Object
```js
{
  id: Number,
  type: 'node' | 'root' | 'note' | 'group' | 'multi',
  x: Number, y: Number,           // координаты центра в пространстве холста
  width: Number, height: Number,   // только для групп
  label: String,
  note: String,                    // текст заметки
  color: String | null,            // цвет фона карточки
  locked: Boolean,                 // блокировка перемещения
  nodes: Array<Number>,            // только для групп — ID узлов внутри
  bg: { color, image, pattern }    // только для групп — индивидуальный фон
}
```

### Edge Object
```js
{
  id: Number,
  from: Number, to: Number,       // ID узлов
  shape: 'straight' | 'bezier' | 'elbow',  // 'curved' в v1 → 'bezier' в v2
  dash: 'solid' | 'dashed' | 'dotted' | 'link',
  width: Number,                   // числовое значение (1, 1.5, 3)
  color: String,
  dir: 'forward' | 'backward' | 'both' | 'none',
  cp1x, cp1y, cp2x, cp2y,         // контрольные точки Безье (null = авто)
  bend: Number,                    // 0.1–0.9, точка изгиба для elbow
  collapsed: Boolean,              // ветка свёрнута
  isLink: Boolean,                 // связующая линия (не иерархическая)
  label: String                    // подпись на линии
}
```

### Глобальное состояние
```
nodes[]              — массив объектов узлов
edges[]              — массив объектов рёбер
idC                  — счётчик ID
zoom, panX, panY     — трансформация вьюпорта
selN                 — id выбранного узла (null если нет)
selE                 — id выбранной линии
selEHandles          — показывать ли рычаги безье
selNSet              — Set: множественное выделение узлов
hist[], fut[]        — стек undo/redo (JSON-снимки, лимит 100)
glDefaults           — дефолты для новых рёбер {shape, dash, width, dir, color}
linkDefaults         — дефолты для связующих линий {shape, dash, width, dir, color}
nodeDefaults         — дефолты для новых узлов {style: {...}, recentColors: []}
autoMode             — авто-расположение
preAutoPositions     — позиции до переключения в авто
linkMode, linkFrom   — режим связывания
branchViewId         — режим ветки
miniVisible          — видна ли миникарта
noteNodeId           — id узла для заметки
trash[]              — корзина удалённых объектов
canvasBg             — настройки фона холста
```

---

## 4. ИНТЕРФЕЙС

### Плавающие кнопки (div.fb)
| ID | Позиция | Функция |
|----|---------|---------|
| `#btn-undo` | верх-лево | ↩ отмена |
| `#btn-redo` | верх-лево | ↪ повтор |
| `#btn-zin` | низ-лево | + зум |
| `#btn-zout` | низ-лево | − зум |
| `#btn-center` | низ-право | ⊙ центр |
| `#btn-mini` | низ-право | 🗺 миникарта |
| `#btn-auto` | низ-право | ⊞/🤚 авто/ручной |
| `#btn-menu` | верх-право | ☰ главное меню |
| `#btn-fs` | верх-право | ⛶ полноэкран |

### Контекстное меню узла (иконки без слов)
📝 заметка · ➕ дочерний · 🔗 связать · ✂️ вырезать · 📋 копировать · 📌 вставить · 🔭 как карту · 🗑 удалить

### Контекстное меню линии (LP Panel)
Назначение: 🔗 Основная / ⛓️ Связующая · Форма: ── / ∿ / ⌐ · Тип: ── / ╌╌ / ··· · Толщина: тонкая / средняя / толстая · Направление: → / ← / ↔ / — · Цвет: палитра · 📌 По умолчанию · ⑂ Применить к ветке

### Главное меню (☰)
🔗 Поделиться · ⬇️ Скачать (JSON-LD / Markdown / PNG) · ➕ Новая карта · Импорт/Экспорт · 🥷 Стелс-режим · Настройки фона · 🗑 Корзина

---

## 5. УПРАВЛЕНИЕ

### Десктоп
| Действие | Результат |
|----------|-----------|
| ЛКМ по узлу | выделить, показать кнопки «+» |
| Клик по тексту узла | редактировать inline |
| ЛКМ + drag по пустому | выделение области |
| Средняя кнопка + drag | панорамирование |
| Колёсико | зум |
| ЛКМ по линии | выделение + рычаги Безье |
| Drag по линии | изгиб линии |
| ПКМ по линии | меню линии (без рычагов) |
| ПКМ по пустому | меню холста |
| ПКМ по узлу | контекстное меню |
| Tab | добавить дочерний |
| Del/Backspace | удалить |
| Ctrl+Z / Ctrl+Y | undo / redo |

### Тачскрин
| Действие | Результат |
|----------|-----------|
| 1 палец | панорамирование |
| Pinch | зум |
| Тап | выделить |
| Двойной тап по узлу | «+» для дочернего |
| Долгое нажатие узла | контекстное меню |
| Долгое нажатие пустого | меню холста |
| Тап по линии | выделение |
| Двойной тап по линии | рычаги Безье |

---

## 6. КЛЮЧЕВАЯ ГЕОМЕТРИЯ И РЕНДЕРИНГ

- `nodeHalfExtents(id)` — размеры через `canvas.measureText()` (НЕ DOM). Для групп: `n.width/2`, `n.height/2`. Для multi: `n.width/2`, `n.height/2`.
- `pillBorderPoint(cx,cy,hw,hh,dx,dy)` — аналитическое пересечение с pill-формой.
- `groupSnapPoint` — привязка линий к 4 кардинальным точкам групп.
- `render()` — двухпроходный: сначала группы → линии → узлы сверху.
- `bzAtDist(e, ti0, dist)` — точка на кривой на заданном расстоянии от границы.
- `lineExitFrom(e)` / `lineEntryTo(e)` — используют касательные к кривой.
- **НЕ использовать** `getBoundingClientRect()` — это архитектурное решение.

### Организация модулей (src/)
- **Ядро**: `state.js`, `init.js`, `utils.js`
- **Геометрия/Отрисовка**: `geometry.js`, `layout.js`, `smart-placement.js`, `selection.js`
- **Создание/Удаление**: `create-delete.js`
- **Управление**: `input-mouse.js`, `input-touch.js`, `drag.js`, `edit.js`, `link-mode.js`
- **UI/Модалки/Панели**: `menus.js`, `menu.js`, `ui.js`, `notes.js`, `trash.js`, `line-panel.js`, `branch-view.js`, `mobile-rename.js`

---

## 7. РЕАЛИЗОВАННЫЙ ФУНКЦИОНАЛ (✅)

- ✅ Интерфейс в стиле Mindomo
- ✅ Плавающие кнопки (без тулбара)
- ✅ Зум, панорамирование, выделение области
- ✅ 4 кнопки «+» на узлах (десктоп) / центральная (тачскрин)
- ✅ Создание узла drag'ом от «+» с направлением
- ✅ Кривые Безье с рычагами + линии-колено (elbow)
- ✅ Стрелки (forward/backward/both/none)
- ✅ Сворачивание/разворачивание веток с превью
- ✅ Контекстные меню узлов и линий (иконки)
- ✅ Глобальные дефолты линий
- ✅ Удаление с переподключением детей
- ✅ Заметки к узлам (редактирование/просмотр)
- ✅ Миникарта с навигацией
- ✅ Авто-расположение (4 направления)
- ✅ Undo/Redo (100 уровней)
- ✅ Режим связывания
- ✅ Режим «открыть как карту»
- ✅ Буфер (вырезать/копировать/вставить)
- ✅ Полноэкранный режим
- ✅ Экспорт: JSON-LD / Markdown / PNG
- ✅ Мобильное переименование (bottom sheet)
- ✅ **Группы** (создание, ресайз, snap-точки, перетаскивание)
- ✅ **Group 2 Logic**: удаление линий-дублей, выравнивание сеткой (os-distribution), динамическая видимость групп, превью групп в branch preview.
- ✅ **Group 3 (Этап 3.1) Logic**: увеличение hit area линий (9→18px), hover glow, endpoint circles, переподключение drag'ом, вставка узла в разрыв (кнопка ⊕ + drag на линию).
- ✅ **Persistence** (localStorage автосохранение)
- ✅ **Импорт/Экспорт JSON** для переноса карт
- ✅ **Кастомизация фона** (цвет, паттерн, изображение)
- ✅ **Стелс-режим** (скрытие UI для презентаций)
- ✅ **Корзина** (восстановление удалённых)
- ✅ **PWA** (Service Worker + Manifest)
- ✅ **Настройки узлов** — иконочный UI: форма, рамка (тип/толщина), цвет (рамки/фона), отступ, прозрачность, размытие
- ✅ **Умолчания узлов** — value-based пины 📌, как в меню линий
- ✅ **Кастомные цвета** — память на 5 последних произвольных цветов (узлы и фон холста)
- ✅ **Адаптивный контраст текста** — автопереключение чёрный/белый при изменении фона узла
- ✅ **Z-ordering при drag** — перемещаемый узел и его ветка поднимаются на верхний слой
- ✅ **Подписи на линиях** — SVG-маска для «разрыва» линии, прозрачный фон, поле только при редактировании
- ✅ **Переключатель назначения линий** — 🔗/⛓️ в LP Panel, конвертация между основной и связующей
- ✅ **Вставка в разрыв двойным кликом** — защита от случайного срабатывания
- ✅ **Мультиузел (гибрид)** (`type='multi'`) — расширяемый узел (по умолчанию 1.5x длиннее), ресайз за 4 угла, широкие hover-плюсики по сторонам, подключение к точным координатам границы (adaptive edge offset).

---

## 8. ИЗВЕСТНЫЕ БАГИ (🐛)

### Критические
_Нет известных критических багов._

### Средние
_Нет известных средних багов._

243: 1. ~~Стрелки и кнопка сворачивания при изгибе~~ — ✅ `edgePt`/`edgeTan` теперь корректно обрабатывают bent straight lines через кубическую Безье
244: 2. ~~Фиксация изгиба при перетаскивании~~ — ✅ контрольные точки сдвигаются вместе с узлами при drag
245: 3. ~~Не сохраняется текст заголовков~~ — ✅ `saveToLocalStorage()` вызывается в `done()` editNode
246: 4. ~~Настройки умолчаний связующих линий~~ — ✅ `mkEdge(isLink)` и `handleLinkClick` используют `glDefaults`
247: 5. ~~Недоступность линий внутри групп~~ — ✅ `pointer-events: none` на `.group-box`. Добавлены 4 `div`-сенсора по периметру для hover-плюсов (v1.4: центр группы свободен для кликов)
248: 6. ~~Прямые линии в превью~~ — ✅ Исправлено (начальное ребро теперь использует mkPathD, остальные — в рекурсии)
249: 7. ~~Наложение меню~~ — ✅ `hideAllMenus()` расширена и вызывается во всех show-функциях
250: 8. ~~Стрелки на расстоянии от объектов~~ — ✅ STEPS увеличен до 160, добавлен бинарный поиск для sub-pixel точности
251: 9. ~~Агрессивное прилипание линий~~ — ✅ Радиус Snap в `findNodeAt` уменьшен. Вытягивание линии от узла к объемлющей группе запрещено (v1.4).
252: 10. ~~Перетаскивание рамки отдельно от узлов~~ — ✅ Средняя кнопка или Ctrl+drag заголовка группы. 
253: 11. ~~Визуальное 'глубокое' подключение выделенных линий~~ — ✅ (v1.4) Ручки Безье (bz-arm) рисуются от границы узла (snap point), а не от его сырого центра (x,y).

### Исправленные в v1.6 (Фаза 2 — Group 1 UX & Bugs)
1. ~~Клик на пункт «Загрузки» закрывает главное меню~~ — ✅ Добавлен `stopPropagation` в `toggleExportSub`.
2. ~~Реструктуризация меню Сохранений~~ — ✅ Перемещено в подменю "Загрузки" с новой иконкой (JSON-LD 👁️🧠).
3. ~~Автофокус ввода~~ — ✅ Теперь при пустном заголовке курсор встает в titleInput, иначе в textInput (или открывается в Read-Only).
4. ~~Контрастность плюсиков и элементов меню~~ — ✅ Изменены CSS border'ы и цвета шрифтов для `.np`, `.group-title`, `.bg-swatch`, `.lpb`.
5. ~~Адаптивность контекстных меню~~ — ✅ Добавлена функция `posMenu` в `menus.js`, проверяющая коллизии с краями экрана (ширину и высоту).
6. ~~Адаптивный glow цвет выделения~~ — ✅ Функция `getLuminance()` теперь динамически задает `--sel-color: #fff` на `document.body` для темных фонов `bg.pattern` и `bg.color`.
7. ~~Выделение нескольких СКМ~~ — ✅ Добавлен обработчик `ev.button === 1` в логику выделения нод. Выделение рамки СКМ также поддерживается.
8. ~~Системное ПКМ~~ — ✅ `window.contextmenu.preventDefault` отключено для элементов `INPUT` и `TEXTAREA`.

---

## 9. BACKLOG ЗАДАЧ (📋)

*Очередность выполнения в плане будет выстроена для максимального качества кода: сначала стабилизация UI и багфиксы, затем усложнение логики перемещений/групп и ядра геометрии, и только после этого — наращивание новых крупных фич и визуального оформления.*

### Группа 1: Багфиксы и мелкий UX (Быстрые победы)
- ✅ Ошибка меню: Клик на пункт «Загрузки» не закрывает главное меню.
- ✅ Реструктуризация меню: Переместить пункт «Сохранить» внутрь пункта «Загрузки».
- ✅ Иконка экспорта JSON-LD: Добавить значок "глаз на фоне мозга" для семантической выгрузки.
- ✅ Автофокус ввода: При двойном клике на объект с пустым заголовком курсор сразу ставится в поле ввода.
- ✅ Контрастность: Усилить контраст значка группы и ободков кнопок добавления узлов.
- ✅ Адаптивность меню: Любое открытое контекстное меню должно целиком умещаться в пределах видимой части экрана.
- ✅ Адаптивный цвет: Цвет выделения объектов должен быть адаптивным к светлому и темному фону холста.
- ✅ Выделение СКМ: Добавить возможность выделения последующих объектов средней кнопкой мыши (сохраняя текущий способ).
- ✅ Системное ПКМ: Разрешить системное контекстное меню (вырезать/копировать/вставить) при редактировании текстовых полей.

### Группа 2: Логика Групп и Веток
- ✅ Удаление связей: Удалять линию между узлом и группой при перемещении этого узла внутрь данной группы.
- ✅ Выравнивание в группе: Кнопка в меню группы для выравнивания объектов внутри нее (оси + равные отступы).
- ✅ Сворачивание группы с веткой: Рамка скрывается только если скрыты все узлы внутри. При наведении на (+) — призрачный контур группы.

### Группа 3: Геометрия линий (Ядро графа)
- [ ] Умолчания связующих линий: Отдельные настройки по умолчанию только для связующих линий.
- [ ] Меню фиксации: Перенести настройки фиксации линий отдельным пунктом в ПКМ-меню линий.
- [ ] Адаптивная фиксация: Переключатель в настройках линий объекта.
- [ ] Выход "Только боковые": Линии выходят только из боковых точек.
- [ ] По умолчанию: Переключатель «по умолчанию» к настройкам каждого объекта.
- ✅ Увеличение области клика: Расширена hit area (9→18px), hover glow + подсветка.
- ✅ Переподключение линии: Drag кружочка-endpoint на другой узел переподключает линию.
- ✅ Вставка объекта «В разрыв»: Кнопка ⊕ в LP-панели + drag узла на линию.

### Группа 4: Наращивание новых фич
- [ ] Голосовой ввод: Кнопка микрофона при вводе названия узла (сохранение: 3 сек тишины) и заметки (10 сек тишины).
- [ ] Открытие ветки: Открыть ветку отдельно как карту на фоне размытия основной карты.
- [ ] Слои: Отметка выборочных объектов (меню узла -> "отметить"). Вывод слоев в главном меню с кнопкой "+" (имя, микрофон, сохранить).
- [ ] Детализация (свернуть всё): В главном меню выбор глубины отображаемых уровней (остальные свернуты). Плюс кнопка "свернуть всё".

### Группа 5: Темы и Оформление
- ✅ Настройки рамок узлов: В меню узлов (толщина, тип линии, цвет, прозрачность, размытость, отступ размера).
- [ ] Темы холста: Тёмная / Светлая / Система.
- [ ] Неоновый режим: Переключатель в меню + палитра (контуры становятся светящимся неоном).
- [ ] Тема "Obsidian": Применение значков, иконок и оформления в стиле Obsidian.

### Дальний горизонт
- [ ] Облачная синхронизация (PouchDB/CouchDB)
- [ ] Полноценное PWA с офлайн-режимом
- [ ] AI-интеграция (анализ/генерация карт через LLM)
- [ ] Плагин для Obsidian
- [ ] API для сторонних сервисов
- [ ] Google Drive / Google Keep интеграция

---

## 10. ТЕХНИЧЕСКИЙ ДОЛГ И РЕКОМЕНДАЦИИ ПО РЕФАКТОРИНГУ

### ✅ [ВЫПОЛНЕНО] Критический: Модуляризация
Монолит в 4300+ строк успешно разбит на логические модули в папках `src/` и `styles/`. Использован "Simple file split" без сложной сборки — для сохранения стабильности `window`-контекста.

**Рекомендуется в будущем (Дальний горизонт):**
- Перевод скриптов на полноценные ES-модули (`type="module"`) или внедрение Vite-сборки.

### Средний: Очистка package.json
- Удалить React, Tailwind, motion и связанные зависимости
- Оставить только Vite для сборки (если используется) или убрать вовсе для single-file подхода

### Средний: Улучшение персистентности
- Миграция с raw localStorage на абстракцию хранилища (подготовка к PouchDB)
- Версионирование схемы данных

### Низкий: Код-стиль
- Добавить JSDoc комментарии к ключевым функциям
- Заменить однобуквенные переменные (e, n, r) на осмысленные имена
- Удалить закомментированный/мёртвый код

---

## 11. ВАЖНЫЕ АРХИТЕКТУРНЫЕ РЕШЕНИЯ (НЕ МЕНЯТЬ БЕЗ ОБСУЖДЕНИЯ)

1. **Измерение размеров узлов** через `canvas.measureText()` + CSS-константы, НЕ через `getBoundingClientRect()`.
2. **SVG (`#svgl`) позади узлов-div** (z-index). Кнопки на линиях видимы через JS-хендлеры.
3. **Корневой узел** — всегда `nodes[0]`, нельзя удалить через «удалить узел».
4. **Связи** (`isLink: true`) — декоративные, не влияют на иерархию (`gCh`, `gPar`, `isVisible`).
5. **Тач vs десктоп** определяется через `isMob() = window.innerWidth < 768 || 'ontouchstart' in window`.
6. **Файл всегда редактируется точечно**, не переписывается с нуля.
7. **Статус стартового узла (`type='root'`) неизменен:** `gPar()` в `utils.js` принудительно возвращает `null` для root-узла и игнорирует `link`-рёбра при вычислении иерархии. Ни одна линия не может сделать root-узел «дочерним».
8. **Fullscreen на Chrome/Wayland:** `requestFullscreen()` API технически срабатывает (события firing, элемент устанавливается), но Chrome на Wayland не разворачивает окно физически. F11 работает. Это системное ограничение Chrome/Linux, а не баг приложения. Решено оставить нативный вызов для совместимости с другими ОС/браузерами. Значок кнопки `⛶` намеренно не меняется.
9. **Рабочий URL:** `http://localhost:8080/`. `file:///` и другие порты — изолированное хранилище, не использовать для разработки.

---

## 12. ИСТОРИЯ РАЗРАБОТКИ

| Этап | Инструмент | Результат | Строки |
|------|-----------|-----------|--------|
| Фаза 1 | Antigravity | Стабилизация: исправлены все 8 критических/средних багов | ~4270 |
| Фаза 1.1-1.4 | Antigravity | Доработка Snapping Radius, полировка Групп (сенсоры границ, drag заголовка) | ~4300 |
| Фаза C | Antigravity | Модуляризация: разбивка монолита на `src/*.js` и `styles/main.css` | 3900 JS + 430 CSS |
| Группа 3 (04-02) | Antigravity | Edge defaults (gl/link раздельно), центрирование при загрузке, вставка объектов в разрыв | — |
| Группа 3 (04-03) | Antigravity/Claude | Иммутабельность root-узла (`gPar`), нативный Fullscreen без иконки, Tooltips для кнопок, унификация dblclick для групп | — |
| Группа 5+Оптимизация (04-04) | Claude Opus | Node Settings UI (иконки, value-based пины, палитра с кастомными цветами), адаптивный контраст текста, SVG-маска подписей, переключатель назначения линий (🔗/⛓️), z-ordering при drag, double-click вставка, `getContrastColor()` в utils.js, persist `nodeDefaults`, удаление мёртвого `pins` | — |
| **Мультиузел (гибрид) (04-05)** | Antigravity | `type='multi'`: гибрид узла и группы. width/height, ресайз за углы, прозрачные hover-плюсики, подключение к точным координатам границы (`edge Offset`). `create-delete.js`, `drag.js`, `geometry.js`, CSS. | — |

---

## 13. ПРАВИЛА ОБНОВЛЕНИЯ ЭТОГО ФАЙЛА

> При каждом значимом изменении в проекте (новая функция, исправление бага, рефакторинг, изменение архитектуры) **ОБЯЗАТЕЛЬНО** обновить соответствующие секции этого файла:
> - Обновить «Версия контекста» и «Дата обновления» в шапке
> - Перенести исправленные баги из секции 8 в секцию 7 (с ✅)
> - Перенести реализованные задачи из секции 9 в секцию 7
> - Обновить структуру данных (секция 3), если менялась схема
> - Обновить архитектуру (секция 2), если менялась структура файлов
> - Обновить номера строк в секции 6, если менялся layout кода
> - Добавить новые архитектурные решения в секцию 11
> - Обновить историю разработки в секции 12
