/**
 * Integreco — single entry point.
 * Imports the virtual bundle that concatenates all source files.
 * The bundle preserves global scope: every function/variable is
 * accessible everywhere, just like the original multi-<script> setup.
 */
import 'integreco:app-bundle';
